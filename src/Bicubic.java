import lib.BalikanAdjoin;
import lib.Cramer;
import lib.Determinant;
import lib.Kofaktor;
import lib.Matrix;
import lib.SPL;

public class Bicubic
{
	public static double bicubic(Matrix matrix, double a, double b)
	// Input matrix 4x4, berisi nilai f(i,j) dan nilai (a,b)
	// Output nilai f(a,b)
	{
		// invers matrix untuk -1<=x<=2 dan -1<=y<=2
	}
}